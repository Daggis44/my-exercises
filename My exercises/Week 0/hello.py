# Simple program to greet the user

name = input("Enter your name: ").strip().title()  # get the user's name and clean it up (strip: remove leading/trailing spaces, title: capitalize first letter of each word)


print(f"Hello, {name}") # say hello to the user


